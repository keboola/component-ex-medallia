[
  {
    "name": "01_run_missing_instance_host",
    "description": "structured run with required root field instance_host missing -> exit 1 (no HTTP).",
    "config": {
      "action": "run",
      "parameters": {
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID",
        "#client_secret": "DUMMY_CLIENT_SECRET",
        "mode": "structured",
        "data_object": "feedback",
        "fields": ["a_customerid"],
        "load_type": "incremental_load",
        "incremental_field": "a_initial_finish_timestamp",
        "page_size": 25
      }
    }
  },
  {
    "name": "02_run_missing_client_secret",
    "description": "structured run with #client_secret (credentials) missing -> exit 1 (no HTTP).",
    "config": {
      "action": "run",
      "parameters": {
        "instance_host": "example.medallia.com",
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID",
        "mode": "structured",
        "data_object": "feedback",
        "fields": ["a_customerid"],
        "load_type": "incremental_load",
        "incremental_field": "a_initial_finish_timestamp",
        "page_size": 25
      }
    }
  },
  {
    "name": "03_testConnection_missing_creds",
    "description": "testConnection with credentials missing -> clean exit 1 (not exit 2), no HTTP.",
    "config": {
      "action": "testConnection",
      "parameters": {
        "instance_host": "example.medallia.com",
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID"
      }
    }
  },
  {
    "name": "04_listObjects_missing_creds",
    "description": "listObjects with credentials missing -> clean exit 1 (not exit 2), no HTTP.",
    "config": {
      "action": "listObjects",
      "parameters": {
        "instance_host": "example.medallia.com",
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID"
      }
    }
  },
  {
    "name": "05_config_error_secret_leak_guard",
    "description": "sentinel #client_secret + missing instance_host -> exit 1; secret must not leak (leak assertion lives in tests/test_unit.py::TestSecretLeakGuard).",
    "config": {
      "action": "run",
      "parameters": {
        "#client_secret": "SENTINEL_SECRET_DO_NOT_LEAK",
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID",
        "mode": "structured",
        "data_object": "feedback",
        "fields": ["a_customerid"],
        "load_type": "incremental_load",
        "page_size": 25
      }
    }
  },
  {
    "name": "06_run_bad_raw_query",
    "description": "raw mode with a query missing $first/$after/pageInfo -> static contract check -> exit 1 (no HTTP; static check runs before any token mint).",
    "config": {
      "action": "run",
      "parameters": {
        "instance_host": "example.medallia.com",
        "api_host": "example.apis.medallia.com",
        "company_name": "example",
        "client_id": "DUMMY_CLIENT_ID",
        "#client_secret": "DUMMY_CLIENT_SECRET",
        "mode": "raw",
        "raw_query": "query { feedback { nodes { id } } }",
        "output_table": "raw_feedback",
        "page_size": 25
      }
    }
  }
]
